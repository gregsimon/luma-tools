Use firebase serve to test locally.

Make sure all tests pass using playwright BEFORE commiting anything to git, or deciding that the task is done.

When adding any kind of new feature also update the help (tooltips, etc) so the user can discover it. You may want to update the README.md also if appropriate.

